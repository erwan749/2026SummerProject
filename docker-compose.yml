services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: blindtest-sql
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "TonMotDePasse123!"
    ports:
      - "1433:1433"
    volumes:
      - blindtest-sql-data:/var/opt/mssql

volumes:
  blindtest-sql-data: